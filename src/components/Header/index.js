import changeDestination from '../../assets/images/direction-board.svg';

const Header = () => (
  <>
    <p>Change destination</p>
    <img src={changeDestination} alt="Change direction" width={24} height={24} />
  </>
);

export default Header;
